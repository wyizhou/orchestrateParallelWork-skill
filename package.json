{
  "name": "orchestrate-parallel-work-skill",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node tests/dashboard-browser.e2e.mjs",
    "lint": "find skills/orchestrate-parallel-work -type f \"(\" -name '*.mjs' -o -name '*.js' \")\" -exec node --check '{}' ';'"
  },
  "devDependencies": {
    "playwright": "^1.62.1"
  }
}
