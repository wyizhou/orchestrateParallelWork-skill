---
name: orchestrate-parallel-work
description: 把具有多个可独立验证工作流、依赖关系或写入面的复杂目标拆成隔离单元；必要时先交付共享基础，再分波次执行、统一集成并独立终验。Use when Codex must plan or coordinate genuinely multi-part work involving task decomposition, workstreams, branches or worktrees, subagents, staged integration, or cross-unit validation. Do not use for small single-output tasks or ordinary validation with no parallel workstreams.
---

# Orchestrate Parallel Work

把并行当作依赖关系的结果，而不是目标。先建立共同基础和清晰契约，只并行真正互不阻塞的单元，最后由协调者统一集成、由独立视角验收。

## 遵守四条底线

1. 不因拆分而扩大用户授权、任务范围或外部影响。
2. 不让两个执行者同时修改同一资源；无法隔离时改为串行。
3. 不把尚未稳定的共享决策复制给多个单元分别猜测。
4. 不以单元各自通过代替整体通过；必须执行集成和终验。

## 0. 确认工作模式

先根据用户请求确认当前工作模式，不要把 Skill 被触发当成执行授权。

- 用户只要求评估、拆解、计划或分工方案：只交付编排方案，不创建执行者、分支、worktree 或外部变更。
- 用户明确要求完成、实现或交付任务：在既有授权范围内执行，不为正常实施步骤重复请求许可。
- 用户要求协调已经开始的工作：先读取实际状态，再从当前状态继续，不重复派发已完成或正在执行的单元。

发现会实质改变结果的缺失决策时请求用户决定；其余低风险细节采用可撤销假设并显式记录。

## 1. 判断是否值得拆分

先评估复杂度、依赖、风险和协调成本。

- 小而明确、单一产物、单一写入面的任务：直接完成，不强行并行。
- 存在多个可独立验证的结果、明显专业分工或耗时工作流：拆成单元。
- 存在共享模型、术语、数据、接口、模板、安全边界或决策：先建立最小基础。
- 单元高度耦合、频繁改同一资源或依赖仍不清楚：先串行消除耦合。

只有当至少两个单元能够在同一波次独立就绪、分别验收且不存在写入冲突，并行收益预计高于协调成本时，才进入并行执行；否则直接完成或先串行解耦。

把“分支”理解为隔离工作流：代码任务可使用 Git 分支和 worktree；研究、分析或文档任务可使用独立上下文、独立证据集、独立文件或明确的章节所有权。不要为了符合名称而给非代码任务创建 Git 分支。

## 2. 冻结总目标契约

在派发前写清以下内容：

- 目标结果和用户真正要解决的问题。
- 明确不做的内容。
- 权威输入、输入快照和不可修改的来源。
- 约束、风险、授权边界和可回滚要求。
- 可以判断通过或失败的整体验收标准。
- 最终产物、交付位置和谁负责集成。

## 3. 建立最小共享基础

仅在多个单元都依赖同一事实时创建基础单元。基础可包括：

- 统一术语、范围、接口或数据口径。
- 输入清洗、样本冻结、目录骨架或共享模板。
- 安全边界、权限模型、测试夹具或验收工具。
- 集成规则、命名规则和输出格式。

要求基础单元本身可验证、可交付、可回滚。先验收基础，再从同一已确认快照启动下游单元。不要把所有前期工作都塞进“基础”，也不要让下游执行者自行补写共享契约。

## 4. 设计工作单元

为每个单元填写一份单元契约：

```text
ID / 名称：
目的：
依赖与起始快照：
负责范围：
禁止触碰的范围：
唯一负责人和隔离方式：
模型 / 推理等级及选择依据：
预期产物：
可观察验收标准：
验证计划与证据：
完成定义：
集成顺序与失败回退：
```

只在单元满足以下条件时独立派发：

- 产物边界清楚，责任只有一个所有者。
- 输入稳定，输出可单独检查。
- 与同波次单元没有写入冲突。
- 失败不会污染其他单元，可撤销或隔离。
- 合并后能为整体目标增加完整、可解释的价值。

### 为每个单元设计验证

派发前为每个单元冻结与任务类型匹配的验证计划，写清可观察的正确行为、验证命令或步骤、通过与失败条件，以及必须交回的证据。可以规定确定性功能和产物要求，但不要为研究、分析或独立验证单元预设应当得出的结论。

选择最小充分的验证组合：

- 软件开发：行为变更运行相关单元测试；缺陷修复增加或运行可复现问题的回归测试；类型或接口变化运行类型检查；代码变化运行项目已有 linter；构建、打包或跨模块变化分别运行构建或集成检查。
- 数据分析：执行数据断言、关键结果复算、来源对账，以及适用的缺失值、异常值、分母和口径检查。
- 研究与信息分析：核验来源真实性、引用与主张的对应关系、关键事实交叉验证和证据完整性，不把预期结论当作验收标准。
- 文档与内容：检查结构、术语、链接、引用、渲染效果和适用的抽样复核。
- 运营、审计或流程任务：优先使用 dry-run、变更前后状态对比、权限检查和回滚验证。

不要要求每个单元都新建测试文件，也不要运行与变更无关的工具。优先复用项目已有的测试、linter、类型检查和构建命令，并遵守适用的 `AGENTS.md`。没有自动化检查时，必须提供可复现的人工检查步骤和原始证据。共享测试夹具或验收工具应作为共享基础并指定唯一写入者。

按依赖关系形成有向无环图，并标注串行基础、可并行波次、集成门和最终门。不要按文件数量或人数机械平均拆分。

## 5. 选择隔离方式

根据任务类型选择最小充分隔离：

- 软件开发：为集成工作建立固定基线；基础通过后，从同一集成提交创建单元分支与 worktree。每个执行者只写自己的 worktree，单元变更先进入集成分支，再由集成分支统一交付。
- 研究与信息分析：为每个问题建立独立上下文、检索范围和证据记录；统一引用规则，避免各单元互相抄结论。
- 数据分析：冻结数据快照、指标定义和查询口径；按指标、假设或数据域隔离查询与产物，最后统一复算关键结果。
- 文档与内容：按章节或独立素材分配所有权，预先固定受众、术语、结构和格式；由一个编辑统一口吻和交叉引用。
- 运营、审计或流程任务：按对象、区域或检查面隔离，优先只读、沙箱或 dry-run；涉及真实外部变更时再次核对授权。

只有在用户或上级规则允许时才创建子 Agent、分支、worktree、外部消息或真实系统变更。

## 6. 分波次并行执行

协调者维护依赖图和单元状态，不与执行者争抢同一写入面。使用 `blocked → ready → active → submitted → accepted → integrated` 表示正常流转；使用 `failed` 表示执行失败，使用 `stale` 表示共享基础变化后原结果已经失效。

### 选择子 Agent 的模型与推理等级

为每个需要创建的子 Agent 单独选择模型和推理等级，并按以下优先级执行：

1. 先读取该单元实际工作目录适用的 `AGENTS.md`；若其中明确规定模型或推理等级，严格采用该规则。只规定其中一项时，另一项再按下一条动态选择。
2. `AGENTS.md` 没有明确规定时，根据任务难度、复杂度、风险、上下文规模、工具调用和验收要求，选择预计能够可靠完成任务且成本最低的组合。允许范围最高为 `gpt-5.6-terra` + `medium`，最低为 `gpt-5.6-luna` + `light`；不要使用高于上限或低于下限的组合。

在允许范围内，优先把边界清楚、低风险、易验证的单元交给较轻量组合；只有任务需要更强的多步推理、跨文件理解、复杂工具协作或高风险判断时才逐级提高，最高不超过 `gpt-5.6-terra` + `medium`。独立 Validator 若由子 Agent 承担，也遵守同一选择规则，但仍保持第 8 节规定的信息独立性。

派发前确认目标模型和推理等级在当前平台实际可用。`AGENTS.md` 指定的组合不可用时，不得静默改用其他组合；优先采用其中明确给出的回退规则，否则改由协调者完成、转为串行，或向用户说明限制。动态选择的首选组合不可用时，只能改选允许范围内仍能可靠完成任务的组合；若没有可用组合，不得静默越过上下限。记录每个单元最终采用的模型、推理等级和简短理由。

仅当下列条件全部满足时启动一个单元：

1. 所有前置依赖已经验收。
2. 单元契约、输入快照和允许写入范围已固定。
3. 同波次不存在资源冲突。
4. 验证计划、通过与失败条件可执行，失败路径明确。
5. 有足够协调容量处理反馈和集成。

给执行者最小但完整的上下文：总目标、该单元契约、共享基础版本、相关原始材料和交付格式。提供已确认的接口和必需的上游验收结果；对需要独立判断的研究、分析或验证单元，不要泄漏其他单元的预期结论。不要把“预期产物”误写成“预期答案”，也不要让执行者自行扩大范围。

要求每个单元交回：

```text
完成内容：
产物位置或提交：
运行过的验证命令或步骤：
验证结果与证据：
未运行的适用检查及原因：
假设与偏差：
未解决问题：
对后续单元或集成的影响：
```

单元失败、超时或越界时，先隔离其结果，不得进入集成。仅在输入和契约仍然有效时原样重试；否则缩小或重写单元契约、改为串行，或形成新的共享基础快照后重新派发。限制重复尝试，不无限重试。

## 7. 逐单元验收并集成

先按单元契约验收，再进入集成。报告“已完成”不是证据；直接检查产物、测试、计算、引用、截图、日志或其他可复现结果。

只有规定的验证已经通过且证据可复现，单元才能标记为 `accepted`。验证失败时不得集成；验证工具不可用时，执行者必须说明原因并提供替代证据，由协调者决定是补充验证、接受并记录风险，还是拒绝该单元。检查测试和验收方法是否真正覆盖目标行为；执行者自测不能代替集成检查和独立终验。

由协调者负责集成：

- 按依赖顺序接收单元，不让执行者相互覆盖。
- 解决接口、术语、数据、格式和行为冲突。
- 每接入一波就运行相关集成检查，及时定位回归来源。
- 若共享基础必须改变，停止受影响单元，形成新快照后重新派发；不要静默漂移契约。
- 记录有意接受的偏差及理由，不掩盖失败。

代码任务中，单元分支应先面向集成分支接受检查；最终仅由集成分支面向正式基线交付。清理分支和 worktree 只能在确认已合并、无未保存产物且授权允许后进行。

## 8. 独立终验

协调者完成集成检查后，再安排未参与实现且默认只读的 Validator，从用户目标出发检查：

- 每个单元的完成定义是否真实满足。
- 单元组合后是否出现冲突、遗漏、重复或口径漂移。
- 端到端结果、边界情况、安全与回滚要求是否通过。
- 关键单元的验证证据是否能够独立复现，测试是否真正覆盖目标行为。
- 最终产物是否可运行、可复现、可理解、可交付。
- 临时资源、分支、上下文或外部状态是否按授权清理。

第一轮独立终验只给 Validator 提供：

- 用户原始目标、冻结的总目标契约和验收标准。
- 权威输入、最终集成产物和必要的运行或检查方法。
- 判断结果所必需的接口、数据口径和安全边界。

第一轮不要提供实现者的自我评价、协调者的预期结论、希望证明的答案、已怀疑的问题或修复说明，避免引导检查方向。Validator 可以读取核验证据所必需的原始记录，但不要用单元的“已完成”报告代替直接检查。

第一轮完成后，可将已知风险、历史问题和修复内容交给同一 Validator 或另一检查者进行第二轮定向复验。若任务本身就是复验某个已知问题，可以直接提供该问题，但不得把“应该通过”作为预期答案。

Validator 只报告证据和可复现问题，不直接修改产物；由协调者组织修复和复验。无法使用独立 Validator 时，由协调者执行同样的终验，并在交付中明确缺少独立复核。

## 9. 面向用户汇报

默认先给精炼的依赖图、波次和验收门；只有进入实际派发或用户要求时，才展开每份单元契约。控制计划成本，不让编排文档吞噬执行时间。

用简洁结果说明：

1. 如何拆分以及为什么可以并行。
2. 是否建立了共享基础及其版本。
3. 各单元、波次和当前状态。
4. 单元验证、集成验证和独立终验结果。
5. 未完成项、风险、偏差和下一步。

不要把代理数量、分支数量或并行度当作成果；成果是经过整体验收、符合用户目标的最终结果。
